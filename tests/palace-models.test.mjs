import test from 'node:test';import assert from 'node:assert/strict';import * as T from '../vendor/three/three.module.js';import {createHall,createModule,materials,THEMES} from '../palace-models.mjs';
test('all eight modules are real non-flat selectable meshes in each era',()=>{for(const theme of THEMES){const mat=materials(theme);for(let i=0;i<8;i++){const g=createModule(i,mat),b=new T.Box3().setFromObject(g),size=b.getSize(new T.Vector3());assert.ok(size.x>.5&&size.y>.2&&size.z>.5,'module must have volume: '+i);let count=0;g.traverse(o=>{if(o.isMesh){count++;assert.equal(o.userData.module,i);for(const value of o.geometry.attributes.position.array)assert.ok(Number.isFinite(value))}});assert.ok(count>=5);g.traverse(o=>o.geometry?.dispose())}Object.values(mat).forEach(m=>m.dispose())}});
test('hall contains seven upgradeable side rooms and full ceiling constellation',()=>{const hall=createHall();assert.equal(hall.userData.bays.length,7);assert.equal(hall.userData.stars.children.length,58);assert.ok(hall.children.length>100);hall.traverse(o=>o.geometry?.dispose())});
